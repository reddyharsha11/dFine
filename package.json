{
  "name": "dfine-dental",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "server": "cd server && npm install && npm start"
  }
}
